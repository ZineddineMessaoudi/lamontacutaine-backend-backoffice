{% extends 'base.html.twig' %}

{% block title %}Médias{% endblock %}

{% block body %}
<div class="container mt-5 col-lg-6 col-xl-4">
      <div class="d-flex justify-content-end">
            <a href="{{ app.request.headers.get('referer') }}" class="btn  btn-outline-secondary">Retour</a>
      </div>
      <h2 class="mt-3">Ajouter des photos</h2>
      {{ form_start(form, {"attr": {"novalidate": "novalidate"}}) }}
            <div>
                  {{ form_label(form.imageFile) }}{{ form_widget(form.imageFile) }}
                  <span class="text-secondary-emphasis">{{ field_help(form.imageFile) }}</span>
                  <div class=" form-error my-2">
                        {{ form_errors(form.imageFile) }}
                  </div>
            </div>
      
            <div class="{% if type is defined %}d-none{% endif %}">
                  {{ form_label(form.event) }} {{ form_widget(form.event) }}
                  <div class="form-error">
                        {{ form_errors(form.event) }}
                  </div>
            </div>
      
            <div class="text-center my-3">
            <button class="btn btn-outline-primary">Valider</button>
            </div>
      {{ form_end(form) }}
{% endblock %}